# ServiceNow — public reconstruction (the vendor)

Orbit has a live PDI: dev406584 (Australia). This folder is the vendor map, not instance secrets.

## Public

- **Now Platform** — ITSM, CSM, HRSD, ITAM, SecOps, etc.
- **Now Assist / Build Agent** — public agent surfaces (PDI welcome mail named Build Agent).
- **Store / plugins** — CSM plugin called out for keel-ingest (`sn_customerservice_case`).

## Unknown
- ServiceNow’s own internal IT (they eat the platform — **not sourced as fact**).
